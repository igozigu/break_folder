# 폴더 평탄화(Flatten) 앱 개발 및 개선 히스토리 (History)

이 문서는 **폴더 평탄화(Flatten) GUI 앱 (`flatten_app.exe`)**의 최초 기획부터 요구사항 변경, 기술적 이슈 해결, 기능 개선까지의 모든 대화 및 개발 과정을 상세히 정리한 기록입니다.

---

## 📌 프로젝트 개요
- **프로젝트명**: `break_folder`
- **GitHub 리포지토리**: [https://github.com/igozigu/break_folder](https://github.com/igozigu/break_folder)
- **핵심 목표**: 복잡한 하위 폴더 트리 구조를 단 한 번의 클릭 또는 드래그 앤 드롭으로 평탄화(Flatten)하여, 대상 폴더 내의 모든 파일(최상위 파일 및 깊은 하위 파일)을 `all_files` 폴더 하나로 무손실 이동하고 빈 폴더를 정리하는 Windows 단일 exe 유틸리티 개발.

---

## 🕒 개발 및 개선 타임라인 요약

### 1단계: 최초 기획 및 기본 CLI 앱 구현
- **사용자 요청**: `walkthrough.md` 설계 문서를 기반으로 평탄화 단일 exe 제작 요청.
- **주요 구현 내용**:
  - `walkthrough.md`에 명시된 핵심 원칙 반영:
    1. **무손실 원칙**: 모든 파일 누락 없이 이동.
    2. **파일명 충돌 방지**: 중복 파일명 발생 시 상대경로를 접미사로 붙여 리네임 (예: `file__sub1.ext`, `file__sub1_2.ext`).
    3. **무한 루프 및 자기 참조 방지**: `all_files` 폴더 및 실행 중인 프로그램 자체는 스캔에서 제외.
    4. **Windows 긴 경로(260자 제한)**: `\\?\` 접두사 자동 적용.
    5. **빈 폴더 삭제**: 이동 후 bottom-up 순회로 빈 디렉터리 정리.
  - Python 표준 라이브러리 기반 1차 콘솔 CLI 앱 구현 및 PyInstaller 빌드 (`dist/flatten_app.exe`).

---

### 2단계: GitHub 리포지토리 생성 & 드래그 앤 드롭 GUI 전환
- **사용자 요청**:
  - 폴더명(`break_folder`)과 동일한 GitHub 원격 리포지토리 생성 및 커밋 & 푸시.
  - exe 파일은 `.gitignore`에 넣지 말고 Git에 함께 업로드.
  - 실행 시 팝업 창 형태의 GUI를 띄우고, **드래그 앤 드롭(Drag & Drop)**으로 대상 폴더를 업로드하며, **[작업 시작]**과 **[작업 취소]** 버튼이 한눈에 보이도록 구현.
- **수정 및 조치**:
  - GitHub REST API를 통해 원격 리포지토리 `igozigu/break_folder` 자동 생성.
  - `.gitignore` 작성 시 빌드 캐시만 제외하고 `dist/` 및 `.exe`는 포함되도록 설정.
  - `tkinter` 기반 GUI 앱으로 전면 개편:
    - **Catppuccin Mocha 다크 테마** 디자인 적용.
    - 눈에 잘 띄는 대형 초록색 **[🚀 작업 시작]** 버튼 및 빨간색 **[❌ 작업 취소]** 버튼 배치.
    - 폴더 드롭 존, 실시간 프로그레스 바, 실시간 작업 로그 텍스트 영역 구성.
    - 작업 비동기 실행을 위해 멀티스레딩(`threading.Thread` + `cancel_event`) 적용.
  - PyInstaller `--windowed` 옵션으로 콘솔 창 없는 GUI exe 빌드 후 GitHub 푸시.

---

### 3단계: exe 파일의 위치를 최상단 루트로 이동
- **사용자 요청**: exe 앱이 `dist/` 폴더 안이 아니라 프로젝트의 최상위 루트 디렉터리에 위치해야 함.
- **수정 및 조치**:
  - `dist/flatten_app.exe`를 프로젝트 최상위 루트(`E:\Github_repo\break_folder\flatten_app.exe`)로 이동.
  - 불필요한 `dist/` 디렉터리 삭제.
  - Git 커밋 및 푸시 후 사용자가 즉시 테스트해볼 수 있도록 exe 자동 실행.

---

### 4단계: 드래그 앤 드롭 오작동 원인 분석 및 `tkinterdnd2` 전면 개편
- **사용자 요청**: *"드래그앤드롭이 작동하질 않고 있어"*
- **원인 분석**:
  1. 기존 `windnd` 라이브러리는 Windows OS 최상위 창(Top-Level Window) 핸들이 아니라 Tkinter의 내부 자식 윈도우 ID(`winfo_id`)를 후킹함.
  2. Windows OLE/쉘에서 마우스 드래그 시 최상위 윈도우(`GetAncestor(hwnd, GA_ROOT)`)에 `DragAcceptFiles` 플래그가 없으면 OS 차원에서 진입 금지(🚫) 아이콘을 띄우거나 이벤트를 완전히 차단함.
  3. 한글 윈도우 환경에서 ANSI/CP949 디코딩 예외 문제.
- **수정 및 조치**:
  1. **`tkinterdnd2` 공식 엔진 도입**:
     - Tcl/Tk의 표준 OLE 드래그앤드롭 패키지인 `tkinterdnd2`(`TkinterDnD.Tk`)를 적용.
     - 드롭 영역뿐 아니라 메인 창 전체 어디에나 폴더를 드래그해도 즉시 인식.
     - 마우스 진입 시 테두리가 초록색으로 밝아지는 시각 피드백(`<<DragEnter>>`, `<<DragLeave>>`) 추가.
  2. **Win32 OLE 대체 엔진 내장 (2중 안전장치)**:
     - ctypes 기반으로 최상위 윈도우 핸들(`GA_ROOT`)을 직접 계산하고 유니코드 API(`DragQueryFileW`)를 사용하는 fallback 루틴 탑재.
  3. **PyInstaller 번들링**:
     - `--collect-all tkinterdnd2` 옵션으로 빌드하여 단일 exe 내부에서도 드래그앤드롭 바이너리(tkdnd DLL/스크립트)가 완벽히 동작하도록 패키징.
  4. 최상단 `flatten_app.exe` 교체 및 Git 커밋/푸시.

---

### 5단계: 불필요한 확인 및 완료 팝업(MessageBox) 일체 제거
- **사용자 요청**: *"이런 류의 추가적인 팝업은 띄우지 말고 진행하고 작업 완료시에도 별도 팝업은 띄우지 말고"* (스크린샷 첨부: 작업 확인 `messagebox.askyesno` 대화상자).
- **수정 및 조치**:
  - 시작 시 뜨던 `messagebox.askyesno`("다음 폴더를 평탄화하시겠습니까?") 제거 -> **[🚀 작업 시작]** 클릭 시 즉시 실행.
  - 작업 완료 시 뜨던 `messagebox.showinfo` / `messagebox.showwarning` 팝업 제거.
  - 이동할 파일이 없거나 취소 시 뜨던 팝업도 모두 제거.
  - 모든 상태 피드백은 메인 창 내의 **상태 라벨**, **프로그레스 바(100%)**, **작업 로그 창**으로만 깔끔하게 일원화.
  - 재빌드, 최상단 exe 교체, Git 커밋/푸시.

---

### 6단계: CSV 로그 파일 생성 제거 & 최상위 파일 포함 평탄화
- **사용자 요청**: *"log.csv파일을 생성하는거 하지말고 해당 폴더에 폴더가 없이 최상위에 있는 파일도 같이 all_files안에 함께 넣어줘 즉, 폴더 안의 모든 파일이 들어가야함"* (스크린샷 첨부: 대상 폴더 내 남겨진 파일들과 생성된 `flatten_log_*.csv` 파일).
- **수정 및 조치**:
  1. **최상위 파일 수집 포함**:
     - 기존에는 최상위 루트에 있는 파일은 이미 평탄화되어 있다고 판단하여 건너뛰었으나(`if full_path.parent == target_dir: continue`), 대상 폴더 루트에 있는 파일도 모두 `all_files` 폴더 안으로 이동하도록 변경.
     - 최상위 파일과 하위 파일 간 동일 파일명이 충돌할 경우에도 덮어쓰지 않고 안전하게 번호 접미사(`_2`, `_3`)를 부여하는 리네임 로직 보완.
     - 결과적으로 평탄화 완료 후 대상 폴더에는 **오직 `all_files` 폴더 하나만 남고 모든 파일이 그 안으로 집결**됨 (프로그램 자체인 `flatten_app.exe` 제외).
  2. **CSV 로그 파일 생성 완전 제거**:
     - 불필요하게 생성되던 `flatten_log_YYYYMMDD_HHMMSS.csv` 저장 로직을 완전히 삭제.
     - 모든 작업 내역과 무손실 검증 결과는 앱 UI의 '작업 로그' 창에만 출력.
  3. 재빌드, 최상단 `flatten_app.exe` 갱신 및 Git 커밋/푸시 완료.

### 7단계: 원본 파일 및 폴더 무삭제·무이동(Copy 모드) 전환
- **사용자 요청**: *"이전에 원래부터 있었던 폴더들은 그대로 최상위 폴더에 남아 있어야 하지 즉, 기존 파일 및 폴더는 건드리지 말고 해당 폴더 및 파일들을 all_files라는 폴더를 생성해서 거기에 넣어두라는 말야 / 기존 파일 폴더 등은 삭제하거나 이동하라는게 아니야 all_files로 복사해서 대상 폴더 안에 파일은 모두 모아서 copy해서 all_files로 복사하라는 말이야"*
- **수정 및 조치**:
  1. **파일 이동(`shutil.move`) → 복사(`shutil.copy2`)로 변경**:
     - 원본 파일과 폴더는 어떠한 수정/삭제/이동도 발생하지 않으며 100% 그대로 보존됨.
     - 대상 폴더 루트의 파일 및 모든 깊이의 하위 폴더 내 파일들을 `all_files` 폴더에 그대로 복사(Copy).
  2. **빈 폴더 삭제 로직 완전 제거**:
     - 기존에 수행되던 빈 폴더 bottom-up 삭제 과정을 전면 삭제하여, 기존의 모든 폴더 트리 구조가 완벽하게 유지되도록 보장.
  3. **UI 및 메시지 명확화**:
     - 메인 UI의 설명 문구를 "원본 파일과 폴더는 그대로 유지하고, 모든 파일을 all_files 폴더로 복사(Copy)합니다"로 갱신.
  4. 재빌드, 최상단 `flatten_app.exe` 교체 및 Git 커밋/푸시 완료.

---

## 📂 최종 파일 구조

```text
E:\Github_repo\break_folder/
├── .git/                      # Git 버전 관리 저장소 (remote: origin/main)
├── .gitignore                 # 빌드 임시 파일 및 캐시 제외 설정
├── flatten_app.py             # 폴더 평탄화 GUI 애플리케이션 전체 소스 코드
├── flatten_app.exe            # 사용자 배포용 Windows 단일 실행 파일 (최신 빌드)
├── walkthrough.md             # 최초 아키텍처 및 요구사항 설계 문서
└── history.md                 # 전체 개발 및 수정 작업 히스토리 (본 문서)
```

---

## 💡 최종 동작 메커니즘 요약
1. **실행**: `flatten_app.exe` 더블클릭 실행.
2. **폴더 지정**: 대상 폴더를 창 어디로든 **드래그 앤 드롭**하거나 `[📁 폴더 찾아보기...]`로 선택.
3. **작업 시작**: 큰 초록색 **[🚀 작업 시작]** 버튼 클릭 (확인 팝업 없이 즉시 시작).
4. **평탄화 복사 수행**:
   - 대상 폴더 루트 및 모든 하위 폴더의 파일 전수 스캔.
   - `all_files` 폴더 생성 및 파일명 충돌 검사 (중복 시 자동 리네임).
   - 모든 파일을 `all_files` 폴더 안으로 **안전 복사(`shutil.copy2`)** (원본은 1바이트도 건드리지 않음).
   - 기존 원본 폴더 및 파일 **삭제/이동 없음** (기존 구조 100% 유지).
   - 무손실 검증 (스캔 파일 수 == all_files 내 복사된 파일 수).
5. **완료 상태**:
   - 별도 알림 팝업이나 CSV 생성 없이, 창 내부 로그와 프로그레스 바로 완료 안내.
   - 원본 폴더와 파일들은 원래 자리에 그대로 남아있고, `all_files` 폴더 안에 모든 파일들의 복사본이 깔끔하게 평탄화되어 집결됨.

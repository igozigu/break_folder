# 폴더 평탄화(Flatten) exe 앱 구현 Walkthrough

## 1. 요구사항 정리

| # | 요구사항 | 상세 |
|---|---|---|
| 1 | exe 위치 | exe는 대상 폴더 트리의 **최상위 폴더**에 위치 (즉, 평탄화할 폴더 자체가 실행 위치) |
| 2 | 결과물 위치 | 최상위 폴더 안에 `all_files` 폴더를 새로 생성하고 모든 파일을 그 안으로 이동 |
| 3 | 재귀 탐색 | 하위 폴더가 몇 겹이든 전부 진입해서 파일을 추출 (깊이 제한 없음) |
| 4 | 무손실 | 파일이 하나도 빠지면 안 됨 (누락 금지) |
| 5 | 최종 상태 | 작업 완료 후 폴더가 하나도 남지 않아야 함 (단, `all_files` 폴더 자체는 결과물이므로 예외) |

## 2. 설계 시 반드시 고려해야 할 함정

단순히 "이동 후 폴더 삭제"만 구현하면 실무에서 아래 문제가 반드시 발생합니다. AI 에이전트에게 구현을 맡길 때도 이 부분을 명시해야 합니다.

- **파일명 충돌**: `A/report.pdf`와 `B/report.pdf`가 같은 `all_files`로 이동하면 하나가 덮어써져 요구사항 4(무손실)를 위반합니다. → 충돌 시 원본 상대경로를 파일명에 반영해 리네임해야 합니다 (예: `report.pdf` → `report__A.pdf`, 재충돌 시 `report__A_2.pdf`).
- **exe 자기 자신**: exe가 최상위 폴더에 있으므로 스캔 대상에서 반드시 제외해야 하며, 실행 중인 exe를 이동/삭제하려 하면 오류가 납니다.
- **all_files 자기 참조**: 재귀 스캔 시 이미 생성된 `all_files` 폴더를 다시 스캔 대상에 포함하면 무한 루프/재이동이 발생합니다. 스캔 시작 전 제외 목록에 등록해야 합니다.
- **긴 경로(260자 제한)**: Windows 기본 API는 260자 이상 경로에서 오류가 나므로 `\\?\` 확장 경로 접두사를 사용해야 합니다.
- **잠긴 파일/권한 오류**: 사용 중인 파일, 읽기 전용 속성 파일 처리 시 예외를 잡아 로그로 남기고 계속 진행해야 하며, 조용히 건너뛰면 안 됩니다 (무손실 원칙 위배).
- **되돌릴 수 없는 작업**: 이동+폴더 삭제는 비가역적이므로 반드시 "미리보기(dry-run)" 모드와 "실행 로그(CSV)"를 남겨야 합니다.
- **빈 폴더만 있는 경우**: 파일이 없는 하위 폴더도 삭제 대상입니다 (요구사항 5).
- **숨김/시스템 파일**: 기본적으로 포함할지 제외할지 정책을 정해야 합니다 (권장: 포함, 단 `desktop.ini`, `thumbs.db` 등은 제외 옵션 제공).

## 3. 구현 스택

- **언어**: Python 3.11+ (사용자 환경에 이미 설치되어 있고 PowerShell/CLI 활용 경험과 일치)
- **패키징**: PyInstaller로 단일 exe 빌드 (`--onefile`)
- **UI**: 별도 GUI 없이 exe 더블클릭 시 "실행 위치 = 대상 폴더"로 동작하는 콘솔 앱 + 진행률 출력. 필요 시 `tkinter`로 확인 팝업(진짜 실행할지 Yes/No)만 추가
- **로그**: 실행 위치에 `flatten_log_YYYYMMDD_HHMMSS.csv` 생성 (원본 경로 → 최종 파일명 매핑, 성공/실패 여부)

## 4. 처리 흐름

1. exe가 위치한 폴더를 `TARGET_DIR`로 지정
2. `TARGET_DIR` 하위를 재귀 스캔하되, `all_files` 폴더(있다면)와 exe 자신, 로그 파일은 스캔 대상에서 제외
3. 발견된 모든 파일 경로를 리스트로 수집 (1차 전체 목록화 → 총 파일 수 확정 → 무손실 검증 기준 마련)
4. `TARGET_DIR/all_files` 폴더 생성 (이미 있으면 재사용)
5. 파일명 충돌 검사: `all_files` 내 대상 파일명이 이미 존재하면 자동 리네임 규칙 적용
6. 파일 이동 (`shutil.move`), 각 건마다 로그 기록 (성공/실패/리네임 여부)
7. 이동 완료 후 원본 폴더 트리를 bottom-up으로 순회하며 빈 폴더 전부 삭제 (`os.walk(topdown=False)` + `os.rmdir`)
8. 최종 검증: 이동 전 파일 수 == `all_files` 내 파일 수 + 실패 건수. 불일치 시 경고 출력 후 프로세스 미종료(사용자가 로그 확인하게 함)
9. 완료 메시지 + 로그 파일 경로 출력

## 5. 테스트 체크리스트

- 3단계 이상 깊이의 하위 폴더 구조에서 정상 추출되는지
- 동일 파일명이 여러 폴더에 있을 때 충돌 없이 모두 보존되는지
- 빈 폴더만 있는 하위 트리가 정상적으로 삭제되는지
- 250자 이상 경로 파일 처리 여부
- 실행 중(잠긴) 파일이 있을 때 나머지 작업이 중단되지 않고 로그에 실패로 기록되는지
- 재실행(exe를 두 번 실행) 시 이미 `all_files`가 있어도 오류 없이 동작하는지
- 이동 전/후 파일 총합 개수 일치 검증

## 6. 빌드 명령 예시

```
pip install pyinstaller
pyinstaller --onefile --console --name flatten_app flatten_app.py
```

빌드된 `flatten_app.exe`를 평탄화할 최상위 폴더에 복사해 실행합니다.

---

## 7. AI 에이전트용 프롬프트 (그대로 복사해서 사용)

아래 프롬프트를 Cursor, Claude Code, GitHub Copilot Agent 등 코딩 에이전트에게 그대로 전달하면 됩니다.

```
당신은 Python으로 Windows용 단일 exe 유틸리티를 만드는 시니어 엔지니어입니다.
다음 요구사항에 맞는 "폴더 평탄화(flatten)" 프로그램을 작성해 주세요.

[핵심 요구사항]
1. 프로그램(exe)은 평탄화 대상이 되는 최상위 폴더 안에 위치하며, 실행 시
   exe가 위치한 폴더(스크립트 실행 시에는 현재 작업 디렉터리 또는
   sys.executable/argv[0] 기준 폴더)를 TARGET_DIR로 삼습니다.
2. TARGET_DIR 하위의 모든 폴더를 재귀적으로 탐색하여 모든 파일을 찾아냅니다.
   깊이 제한 없이 몇 단계든 들어가야 합니다.
3. TARGET_DIR 바로 밑에 "all_files"라는 폴더를 생성하고, 찾아낸 모든 파일을
   그 폴더 하나로 이동시킵니다. 파일이 한 개도 누락되면 안 됩니다.
4. 이동 후에는 원래 있던 모든 하위 폴더(빈 폴더가 된 폴더)를 전부 삭제하여,
   최종적으로 TARGET_DIR 안에는 "all_files" 폴더와 exe 파일, 로그 파일 외에는
   폴더가 하나도 남지 않아야 합니다.

[반드시 처리해야 하는 예외 상황]
- 파일명 충돌: 서로 다른 폴더에 동일한 파일명이 있으면 all_files로 옮길 때
  덮어쓰지 말고, 원본 상대경로 일부를 파일명에 붙여 리네임하세요
  (예: sub/report.pdf -> report__sub.pdf, 그래도 충돌하면 뒤에 _2, _3 등을
  붙여 절대 덮어쓰지 않도록 하세요).
- exe 자기 자신과 프로그램이 생성하는 로그 파일은 이동 대상에서 제외하세요.
- 이미 존재하는 "all_files" 폴더는 재귀 스캔 대상에서 제외하세요
  (자기 자신을 다시 옮기는 무한 루프 방지).
- 260자 이상의 긴 경로에서도 동작하도록 Windows 확장 경로(\\\\?\\ prefix)를
  처리하세요.
- 사용 중이거나 권한 문제로 이동에 실패한 파일이 있으면 예외를 잡아
  로그에 "실패"로 기록하고 나머지 파일 처리를 계속하세요. 조용히 건너뛰면
  안 됩니다.
- 파일이 없는 빈 폴더도 전부 삭제 대상입니다.

[안전장치]
- 실제 이동/삭제 전에 "찾은 총 파일 수"와 "삭제될 폴더 목록"을 콘솔에
  출력하고, 사용자가 y/n으로 확인해야 실제 작업이 시작되도록 하세요.
- 작업 완료 후 TARGET_DIR에 flatten_log_YYYYMMDD_HHMMSS.csv 파일을 생성하여
  각 파일의 (원본 상대경로, 최종 파일명, 성공/실패, 리네임 여부)를 기록하세요.
- 마지막에 "이동 전 파일 수"와 "all_files 내 파일 수 + 실패 건수"를 비교해
  일치하지 않으면 경고를 출력하고 로그 확인을 안내하세요.

[구현 방식]
- Python 표준 라이브러리(os, shutil, pathlib, csv, datetime)만 사용하세요.
- 콘솔 기반 CLI로 작성하고, 진행 상황(현재 처리 중인 파일, 전체 대비 진행률)을
  주기적으로 출력하세요.
- 코드는 flatten_app.py 하나의 파일로 작성하고, 마지막에
  `pyinstaller --onefile --console --name flatten_app flatten_app.py`
  로 빌드 가능한 형태로 완성해 주세요.
- 함수 단위로 명확히 분리하세요: scan_files(), plan_renames(),
  move_files(), remove_empty_dirs(), write_log(), verify_counts(), main().

완성된 전체 코드를 출력해 주세요.
```

이 프롬프트를 그대로 코딩 에이전트에게 전달하면 요구사항 1~5를 모두 만족하는 `flatten_app.py`가 생성되고, 이를 PyInstaller로 빌드하면 요청하신 exe가 완성됩니다.
